🧭 权限数据流核心逻辑（推荐顺序）
User（登录后） → Role → Permission → Menus / Routes / Buttons

💡 小总结
能力 方法 用法
判断是否有某个角色 hasRole(role) hasRole("admin")或者hasRole(["admin"])
判断是否有某个权限 hasPermission(perm) hasPermission("users:edit")或者hasPermission(["users:edit"])
综合判断角色 + 权限 hasAccess(roles, perms) hasAccess(["admin"], ["dashboard:view"])
